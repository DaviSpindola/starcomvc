const styles = theme => ({
  root: {
    marginTop: 10,
    marginBottom: 10,
    marginLeft: 10,
    marginRight: 10,
    maxWidth: 200,
    "&:hover": {
      transform: "scale(1.1)"
    }
  }
});

export default styles;
